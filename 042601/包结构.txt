com.ruide.camera
├── session                    // 【新增】会话管理层
│   ├── CameraSession          // 会话纯数据类
│   ├── CameraSessionManager   // 核心控制面（唯一入口）
│   ├── SessionState           // 状态枚举
│   └── ErrorType              // 错误类型枚举
│
├── device                     // 【新增】设备管理层
│   ├── CameraDevice           // USB 设备驱动 + 帧分发
│   ├── CameraDevicePool       // 设备缓存池（productId 维度）
│   └── OpenResult             // 异步打开结果
│
├── usb                        // 【已有】USB 监听层
│   ├── UsbDeviceMonitor       // USB 插拔监听封装（从 GlobalCameraManager 抽离）
│   ├── USBMonitor             // 【已有】底层 USB 监听
│   ├── USBDeviceSelector      // 【已有】USB 设备选择器
│   ├── UVCCamera              // 【已有】UVC 相机驱动
│   ├── CameraListener         // 【已有】相机监听接口
│   ├── IFrameCallback         // 【已有】帧回调接口
│   └── Size                   // 【已有】分辨率数据类
│
├── event                      // 【新增】事件层
│   ├── CameraEvent            // 对外业务事件
│   ├── InternalEvent          // 内部系统事件
│   ├── CameraEventBus         // 双层事件总线
│   ├── CameraHardwareCallback // 硬件回调接口
│   └── DisconnectReason       // 断开原因枚举
│
├── UvcCmdConstant             // 【已有】UVC 命令常量（保持原位置）
│
com.ruide.service              // 【已有】服务层
├── CameraSessionRegistry      // 【修改】客户端归属管理（SSOT）
├── ScreenRecordManager        // 【修改】录像管理
├── MediaStreamService         // 【已有】推流服务
│
└── middleware/impl             // 【已有】AIDL 实现层
    └── ImpCAMService          // 【修改】AIDL 门面（精简）